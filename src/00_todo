  ! is[a-z] をリプレース。


  o edbuf 0 を Vz 同様取り込みバッファにする
  o edbuf を動的に
  o ブロックバッファなどもラインバッファ同様に管理する

  o ファイラーの根本的な rewrite

  ! ^L で取得する文字列を regexp で問題ないような文字列に変換
  o ^L で取得した検索文字列を検索文字列ヒストリーに加える

  o 方向感応チェックを真面目に
  o page up/down、roll up/downのひっかかり処理の違い。
    最上行、最下行にpageupした後、したにスクロールしようとしたときなど....
    . 上下左右のカーソル移動をひっかかりの変更タイミングに

  o ブロックの[ESC]S/[ESC]A/^QF/^QA/^Lなど

  o 迷子のプロファイル

§ まったり

  o ESCAPE などの戻り値
  ? csr_lenew/csr_leupdate を確認

  ! ソースの大幅な整理
    ! 0startと1startが混在しているのをなんとかする。
      完全に0startに統一。
    ! 各種識別子のネーミング
    o C++コメントをどうするか....。
    o 適切なコメント。

  o 作り直す/思いっきり書き直す。

    o 不必要な大域変数などを根絶する。
    o 新旧いり混じっているせいで不安定な部分がないか調べる。
    o メモリリークなどを起こしてないか調べる。

    ! buffer overrun 対策の方法を変更する。
      従来のやり方はある一定のサイズでカットするやりかた。
      バッファを固定せずに動的にあつかうようにする。

    o 可搬性で自分が汚してる部分をなんとかしなくてはならない。
      ? ふる〜い処理系のためANSI-C/C++ -> K&R

  o 初期化関数の整理
  o op_のそれぞの動作のatomicな要素のあらいだし。
  o エラー処理をまともに行う。
    o エラーを真面目に検出する。
    o エラーが出た後のフローをまじめに考える。
    o エラー時に使える安全なメモリの確保。
  o manのインストール先問題
  ? make tar/lzhなどで、アーカイブの中のディレクトリ名に
    バージョンナンバーが反映されるようにする。


§ 優先順位: 中

  o Vz complete
    o ^[A 既存のファイルへの追加書き出し

  o ne2 復活
    o multi search/replace
    o bracket search
    o help

    o history 保存
    o OP.

  o ee みたいなガイドを実装する。
    タイトルの復活

  o readonly
    o ロックされたファイルは readonly にする
    o ファイルの二重化

  x ^[s で最初のファイル名から rename された後での
    ファイル書き込まれチェックがおかしくなる。

  o 全体マーキング
    o search 色分け
    ? TAB 色分け
    o inclemental search

  o le処理系を綺麗にする。
    o ライブラリ化
    o 上書きモードで2bytes文字、tabに完全に対応する。
    o tab -> space に再対応。
      . 既存のテキストのtabをスペースに変換する機能。
      o 打ち込んだtabをspaceに変換して登録する機能。
      o カーソルの位置を指定できるようにする。

    o GetS、HisGets
      o きれいにする。

    o legets
      o complete function(callback)を指定する。
      o historyの登録の仕方を考える。
        同じhistoryは置き換えるようにする。
      o history移動をした後のカーソル位置を正規化するように。

      ? 入力関数をWindow表示。


  ? プロファイルを複数パターン用意？
    o 緊急時用
    o [ESC]Q 用
    o オートセーブ
  o インクリメンタルサーチ。

§ 既に終ったもの。

  - EUC/JIS/SJISを入力できるようにする。
  - EUC/JIS/SJISを出力できるようにする。
  - 各種CRLFに対応する。
  - ワイルドカードの処理をやめる。
  - システムバーを改良する。
  - 単品のブロックコマンドでいちいち画面反転をしなくてもいいようにする。
  - ブロックバッファをスタックにする。
  - ブロックバッファをファイルに持たない。
  - ブロックを複写するコマンドの装備
  - 解析処理をもっと柔軟に。
  - カスタマイズできる項目を調べつくす。
  - keypadを登録できるようにする。
  - キー登録方式を変更する。
  - Defaultの方式を変更する。
  - 現時点の方式に沿った設定ファイルを書き直す。
  - helpを無くす。
  - オプション設定で編集中の文字コードを変更できるようにする。
  - ページングをモーダルにする。
  - 行頭でリターンを押すと、
    次のカーソル位置がインデントの数だけ移動してしまう仕様。
  - インデント処理がおかしいような気がする。
  - 2ストロークキーで、ESCキーのタイムアウトを無視する。
  - key処理、*_op処理
  - CR/LFの判定方式を変更した。
    以前の方式では小さなファイルを操作する時にCR/LFと強制認識されていた。
  - ホストの時のような、*_opを書き出すユーティリティーの移植、サポート。
  - EUC/JIS/SJIS,CRLFなど、編集中のモードを変更できるコマンドを追加する。
  - renameコマンドの装備。
  - ^[Lのようなclose&openを装備する。
  - まだ置換をしていない状態で^QOすると暴走するのをなんとかする。
  - ^QAをVzと同様にする。
  - Vzの^QSと同じ、検索文字列の設定だけのSearchInを装備する。
  - paging mode切替え装備。
  - 登録する関数(SearchIn/SearchInFoward/SearchInBack)によって、
    ページングモードを利用するかどうかを変えた。
  - ブロックの複写、ヤンクバッファの削除を装備。
  - ブロックのペーストのメモリ開放忘れを修正。
  - 行jumpコマンドを装備する。
  - 行マーク＆ジャンプの装備。
  - [ESC]Sで確認をとるようにする。
  - [ESC]Xで確認を一つ一つとるようにする。
  - フリーカーソルの時、文字コードの表示がおかしくなる時があるのを修正。
  - ブロックコマンドが最終行から開始すると全てのコマンドが失敗する。
  - ブロックバッファで、範囲指定が有効かどうかを確認する
    ブロック指定開始後に削除などをした場合
  - ブロックのtop/endコマンドを装備する
  - editlineにsize_t n;を導入
    Reallocを書き直す
  - ^[S を、ファイル名を指定してセーブにする。
  - 画面最上部にいるときに画面左はしでBSすると
    表示が食い違うNxEditオリジナルからあったバグの修正
  - 削除buffer関連を新しい物に
    - 削除バッファで行またぎができるように
  - ^Pで^@を入力するととんでもない事になる
  - LINUXで日本語処理がおかしい理由をさぐる
  - 行単位で消える現象
  - ホストの時のようなVARをサポートする
  - ジャンプ/検索などをした後、カーソル位置をなるべくkeep
  - rolldownの動作がVzと違う
    最下行の時、画面いっぱいまでスクロールしない
  - setlxで横移動する距離をTAB位置で補正できるようにする
  - lists_addからGetListを減らす
  - lists_deleteからGetListを減らす
  - 行編集の取消コマンド(^QL)の装備
  - ブロック(移動による延び縮み)の処理をVz同様にする
  - システムバーに、編集ファイルサイズを表示する
  - メニューの全書き直し
  - シグナルで落ちる時にSegV出るのを修正
  - safetyなprofile書き込み関数の装備
  - profileの読みだし
  - profileのための保存ファイルが消えた時の処理
  - 新型lineedit処理系
  - 汎用le処理系とlegetsを分離する
  _ cursor/lineなどの処理をきっちりle処理系で書き直す
  - 新search/replace処理系
    - 置換時、確認を、「置換する(Y)/置換しない(N)/確認をしない(A)」に
    - search関連の変数をef構造体に
    - 関数を整理する
    - ______みたいな行でprev searchすると最初と最後の_しか反応しない
    - ^QO/Multi Search/Replace専用のバッファ(全体で一組)を用意する
    - nocaseに対応する
  - SearchInNext/Prev
  - 新トークン処理関数
  - 検索文字列の取得(^L)
  - 編集中に、その元ファイルの読みだしpermissionを切ってから、
    ^[Uで、refreshしようとするとSegVなどで落ちる
  - ^[I ファイルの挿入を装備する
  - term.h/term.c/machine.hなどをFDclone 1.03gのものにさしかえる
  - 5curses.hを無くして、disp処理系をでっちあげる
  - 同じ文字へ置換する時に永久パターンにハマるバグ
  - ^P コントロールコード入力時に、aやAで^Aが入力されるように
  - 新規ファイルオープン時に画面表示更新するのを忘れていた
  - ファイラーをmenu処理系を利用するように書き直す
  - profileに付けてるマジックナンバーを変更する
    profile versionを付ける
  - ^Pなどの時に最下行にメッセージが残るのをなんとかする
  _ 座標を記録して、無駄なmoveを発生しないようにする
  _ 属性を記録して、無駄な属性変更を起こさないようにする
  - 読み終ったprofileの削除機能
  - オプションにホストの時のval_tのような汎用変数を導入
  - ANSI-colorの使用が可能かどうか
  - 起動時にviと同様の行番号指定機能
  - カーソルが下の方にある時にメニューの出現位置が
  - menuでアルファベットをおすとひとつずれるバグ
  - ^[sで、ファイル名が同じ状態でセーブする場合は、ctを更新する
  - ファイルの入出力から漢字処理を分離する
  - 既にopenされてるファイルを^QGするとちゃんとジャンプしないバグ修正
  - SIGARLMでプロファイル保存を行う
    自動プロファイル保存ができる(準備)
  - 自分の端末が無くなった時に安全に終了できるよように
  - autoconfを利用する
  - ソースコードディレクトリの分離
  - 半角カナを表示する
  - 半角カナを入力できるようにする
  - reg_pathなどを書き直し
  - 直前のカーソルの位置
  - ^Q1〜4 ^K1〜4
  - データ保存ディレクトリ .ne/の作成
  - csrse.ed
  - !Dでのtermcap getに対しデフォルトを導入する
  - sysinfo.c_* が正しく反映されていない
  - 同一ファイル編集に対する処理をいれる
  - 保存先チェックを[ESC]Sの時忘れている
  - cursor 置場
  - 行末の CR mark が暴れるバグ
  - ブロックの始点、終点を返す関数の装備
  - block_pasteで最後の行に纏わる問題の解決
  - 方向感応
  - 複数のファイルをまたぐ場合に検索がおかしくなる
  - 2byte 文字を置換した時に、余計な文字をけしてしまうバグ
  - レポート
  - プロトタイプ宣言を真面目に行う

